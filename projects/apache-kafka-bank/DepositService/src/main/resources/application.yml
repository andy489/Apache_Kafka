server:
  port: 0

spring:
  application:
    name: DepositService
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: amount-deposit-event
      isolation-level: READ_COMMITTED
      properties.spring.json.trusted.packages: com.appsdeveloperblog.*